# Optional capabilities

`optional.capabilities/v1` models Voice, Computer, Learning Journey and
Insights as capabilities with independent reason codes. The UI can advertise
the entry point without claiming a backend, microphone, Computer lease or
analytics report exists.

When unavailable, the user sees the exact remediation. Opening a capability
does not activate a provider, Bot or agent session.
